import { SortList } from '../SortList/SortList';

// import styles from './SideBar.module.scss';

const SideBar = () => {
  return (
    <div>
      <SortList />
    </div>
  );
};

export { SideBar };
